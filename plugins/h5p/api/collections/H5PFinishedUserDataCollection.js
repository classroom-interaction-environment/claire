export const H5PFinishedUserDataCollection = {
  name: 'h5p-finished-user-data',
  collection: null,
  schema: {
    score: Number,
    maxScore: Number,
    openedTimestamp: Number,
    finishedTimestamp: Number,
    completionTime: Number,
    contentId: String,
    userId: String
  }
}
